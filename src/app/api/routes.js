const urlBase = `http://localhost:8080`;

const urlFactura = `${urlBase}/factura`;
const urlDetalle = `${urlBase}/detalle`;
const urlDetalleByFactura = `${urlBase}/detalle/factura`;
const urlProducto = `${urlBase}/producto`;